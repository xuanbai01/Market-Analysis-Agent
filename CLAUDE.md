# CLAUDE.md

Guidance for Claude Code (claude.ai/code) when working in this repository.

> **Product context:** this repo is the **AI Equity Research Assistant** — a FastAPI backend exposing `POST /v1/research/{symbol}` that returns a Pydantic-typed citation-backed research payload, plus a React + Vite frontend rendering it as a **symbol-centric dashboard at `/symbol/:ticker`** (Phase 4, in progress). Single agent + well-designed tools, free data only, citation discipline non-negotiable. **Visual-first, delta-driven** product shape (charts > prose), with **adaptive layouts** for distressed names. The original v1 vision (real-time multi-agent platform, Discord bot) was cut by [ADR 0003](docs/adr/0003-pivot-equity-research.md); the visual-first commitment (no Morningstar-narrative chase) is in [ADR 0004](docs/adr/0004-visual-first-product-shape.md); the pivot from generated-report-page to symbol-centric-dashboard is in [ADR 0005](docs/adr/0005-symbol-centric-dashboard.md).
>
> **System design:** [`design_doc.md`](design_doc.md) (root) is the source of truth for scope, budget, stack, and roadmap. Read it before making non-trivial changes.
> **Active tasks:** `tasks/todo.md`. Lessons: `tasks/lessons.md`.
> **Architecture Decision Records:** `docs/adr/` — **read [ADR 0003](docs/adr/0003-pivot-equity-research.md), [ADR 0004](docs/adr/0004-visual-first-product-shape.md), and [ADR 0005](docs/adr/0005-symbol-centric-dashboard.md) before proposing surface-shape changes.**

## How this file works

The `@imports` below pull in modular docs — one concern per file — so a single CLAUDE.md does not balloon. Keep this file short; edit the imported docs instead.

---

@docs/commands.md

@docs/architecture.md

@docs/conventions.md

@docs/testing.md

@docs/security.md

@docs/workflow.md

---

## Current state

- **Phase 1 done:** FastAPI scaffold, async SQLAlchemy 2.0 + asyncpg, Alembic, real yfinance ingest, RSI/SMA technicals, RFC 7807 errors, A09 external-call logging, Fly.io + Neon deploy, push-to-deploy via GitHub Actions.
- **Phase 2 done:** `POST /v1/research/{symbol}` is the primary endpoint. Citation-enforcing schema (`Source` / `Claim` / `Section` / `ResearchReport`). 9 free-data tools (`fetch_news`, `fetch_fundamentals`, `fetch_peers`, `fetch_edgar`, `parse_filing` × 4, `fetch_earnings`, `fetch_macro`). Cost-tier-routed LLM client (Haiku triage + Sonnet synth, currently synth-only). Same-day cache (default 7-day window). Per-IP rate limit (default 3/hour, post-cache). Real-LLM golden eval at factuality 0.97.
- **Phase 3.0 done (PR #31):** shared-secret bearer auth dep on `/v1/research/*` (`BACKEND_SHARED_SECRET`), CORS middleware (`FRONTEND_ORIGIN`), `GET /v1/research` paginated list.
- **Phase 3.1 done (PR #32; deploy held):** React + Vite + TS frontend under `frontend/`. Login screen, dashboard, report renderer, past-reports sidebar. Vercel deploy held until Phase 3 visual-depth ships.
- **Phase 3.1 schema done (PR #35):** `Claim.history: list[ClaimHistoryPoint]` added to Pydantic + Zod. Backwards-compat default `[]` so pre-3.1 cached rows round-trip unchanged.
- **Phase 3.2 done (PRs #36 → #40):** all four data-tool history extensions shipped. `fetch_fundamentals` ships 16 history-bearing claims (per-share growth, margin trends, cash flow components, balance sheet trend, ROE/ROIC TTM); `fetch_earnings` ships 3 history-bearing claims with ~20Q lookback; `fetch_macro` ships per-series histories with ~36 monthly observations via FRED `frequency=m`. All three providers converged on a `(values, history_map)` tuple shape.
- **Phase 3.3 done (frontend visualization):** 3.3.A (PR #41), 3.3.B (PR #42), 3.3.C (PR #43) all merged. `Sparkline` + Trend column, `SectionChart` for Earnings / Quality / Capital Allocation / Macro, `PeerScatter` for the Peers section. Main bundle 76.92 KB gz; recharts hoisted into a shared 100 KB chunk used by both lazy chart components.
- **Phase 3.4 done (eval rubric history) (PR #44):** `_claim_numeric_values` widens to yield each `Claim.history[*].value` so trend prose like "EPS rose from 1.40 to 2.18" matches even when the endpoints aren't the snapshot.
- **Phase 4 active (symbol-centric dashboard rebuild):** Per [ADR 0005](docs/adr/0005-symbol-centric-dashboard.md). 8–10 weeks across 4.0–4.8.
  - **4.0 done (PR #46):** Strata token system, Inter + JetBrains Mono, `react-router-dom@6`, `SidebarShell`, `RequireAuth`, `LandingPage`, `SymbolDetailPage`, dark-theme restyle of existing components.
  - **4.1 done (PR #47):** Real `HeroCard` (3-column, price chart + featured stats + meta) + `EarningsCard` (20Q EPS bars + beat-rate headline) + new `LineChart` and `EpsBars` SVG primitives. Backend: `GET /v1/market/:ticker/prices?range={60D,1Y,5Y}` with read-through `candles` cache; `fetch_fundamentals` adds `name`/`sector_tag`/`fifty_two_week_high`/`fifty_two_week_low`; orchestrator lifts `name`/`sector` to `ResearchReport` top-level.
  - **4.2 done (PR #48):** `QualityCard` (3 metric rings ROE/ROIC/FCF + multi-line gross/op/FCF margin chart + hybrid 6+expand to 16 claims) + `ValuationCard` (4-cell matrix P/E TTM / P/E FWD / P/S / EV/EBITDA with peer-percentile bars) + `PeerScatterV2` (hand-rolled SVG, 3 axis presets — replaces 3.3.C Recharts version). New SVG primitives `MetricRing` + `MultiLine`; new extractors `quality-extract.ts` + `valuation-extract.ts`; `peer-grouping.ts` extended with axis-pair-generic helpers.
  - **4.3.A done:** `PerShareGrowthCard` (5 per-share series rebased first=100, MultiLine + 5 growth-multiplier pills) + `CashAndCapitalCard` (cross-section: CapEx/SBC + Cash/Debt MultiLine + Net cash highlight) + `RiskDiffCard` (inline 4-row horizontal-bar SVG of added/removed/kept/char-delta + prose framing "expanded/shrank/stable") + `MacroPanel` (vertical stack of mini area-chart panels, one per FRED series). New extractors `growth-extract.ts` + `cash-capital-extract.ts` + `risk-extract.ts` + `macro-extract.ts`. All-frontend; backend untouched. Main bundle 92.93 KB gz (+2.7). 522/522 backend + 262/262 frontend tests.
  - **4.3.X done (PR #51 — data correctness pass):** `Claim.unit` field (Pydantic + Zod literal) drives a unit-aware `formatClaimValue` so ROE 1.41 → "141.00%", dividend yield 0.39 → "0.39%", capex/share 0.16 → "$0.16". `data_ingestion` upserts `symbols` before candles so `GET /v1/market/AAPL/prices` returns 200 (was 500 for any non-seeded ticker). `backfill_top_level_metadata` lifts `name`/`sector_tag` claims to top-level for pre-Phase-4.1 cached rows. `PerShareGrowthCard` drops the duplicated section narrative. Cosmetic backlog: HeroCard exchange chip, ValuationCard "n = X peers" annotation, QualityCard MARGINS sub-kicker with inline values, MultiLine monotone curves. LoadingState gets a ghost skeleton. Main bundle 93.95 KB gz (+0.97). 531/531 backend + 277/277 frontend tests. See [tasks/dogfood-2026-05-03.md](tasks/dogfood-2026-05-03.md) for the bug catalogue that motivated the PR.
  - **4.3.B done (PR #52 — Risk Haiku categorizer):** see Phase 4.3.B section in `tasks/todo.md`.
  - **4.3.B.1 done (PR #53 — layout polish + range pills + responsive SVGs):** ``sliceForRange`` slices the hero's 60D price feed client-side per pill so 1D / 5D / 1M each render a visibly different chart (was: all 4 sub-3M pills showed the same 60-bar chart). ``EarningsCard`` grows a ``RecentPrints`` mini-table (last 4 quarters: period · actual · estimate · surprise %) and ``PerShareGrowthCard`` grows CAGR sub-lines under each multiplier pill — both close most of the blank-canvas gap to their taller row-2/row-3 partners. ``LineChart``, ``EpsBars``, ``MultiLine``, and ``RiskDiffCard``'s aggregate bars switch from hardcoded ``width`` props to ``width="100%"`` with viewBox preserving internal coords — fixes chart elements escaping the card border on narrow viewports. Main bundle 94.46 KB gz (+0.51). 531/531 backend + 295/295 frontend tests.
  - **4.3.B.2 done (PR #54 — price chart axes + hover tooltip):** ``LineChart`` adds two opt-in props ``showAxes`` (y-axis min/max prices + x-axis first/last dates, UTC-formatted) and ``showTooltip`` (mousemove → guide line + nearest-point dot + floating tooltip with exact date + price; mouseleave clears). HeroCard sets both to `true` so the hero price chart is no longer decorative. Bonus: rescues PR #52's per-category bars conditional render that was silently dropped by the PR #52 ↔ #53 merge — only the aggregate 4-bar SVG was rendering on main. Main bundle 95.71 KB gz (+1.25). 544/544 backend + 314/314 frontend tests.
  - **4.4.A done (PR #55 — News + Business + ContextBand):** new ``fetch_business_info`` tool (yfinance Ticker.info → summary + HQ + employee count) and ``fetch_news`` tool (queries `news_items` for last 7 days, runs Haiku categorizer → 7 buckets × 3 sentiments per headline). Both wired into the orchestrator's TOOL_DISPATCH. Frontend: ``BusinessCard`` + ``NewsList`` (filter pills + sentiment dots + "View N more") + ``ContextBand`` wrapper between Hero and the row-2 grid. Main bundle 97.06 KB gz (+1.35). 565/565 backend + 336/336 frontend tests.
  - **4.4.B done (PR #56 — per-card section narratives):** new ``Section.card_narrative: str | None`` field (Pydantic + Zod) + extended ``SectionSummary.card_narrative`` on the synth tool schema. Sonnet's system prompt grows a 7th rule + style-example block that frames the card_narrative as a 1-2 sentence headline+delta tagline distinct from the broader 2-4 sentence ``summary`` ("Loss is narrowing. EPS −3.82 → −0.78 over 20Q"). New ``NarrativeStrip`` SVG-less primitive renders it at the bottom of QualityCard / EarningsCard / PerShareGrowthCard / RiskDiffCard / MacroPanel; null / undefined / empty / whitespace all hide the strip uniformly. Eval rubric's ``score_factuality`` now polices ``card_narrative`` alongside ``summary`` so the LLM can't dodge the rubric by writing hallucinations into the card-strip. Main bundle 97.17 KB gz (+0.11). 577/577 backend + 353/353 frontend tests.
  - **4.5.A done (PR #57 — layout signals + header pills + hero metric swap):** new ``LayoutSignals`` Pydantic model on ``ResearchReport`` carrying five derived flags — ``is_unprofitable_ttm`` / ``beat_rate_below_30pct`` / ``cash_runway_quarters`` (float | None) / ``gross_margin_negative`` / ``debt_rising_cash_falling``. New pure ``app/services/research_layout_signals.py::derive_layout_signals`` reads claim values via description matching. Cash runway = max(net_cash, 0) / |FCF TTM burn|, computed only when FCF TTM < 0; clamps to ``0.0`` when net cash is already negative + burning. Orchestrator runs derivation as a final step on fresh reports; ``backfill_layout_signals`` helper recomputes for cached pre-4.5 rows (never overwrites a populated value). Frontend mirrors the schema with per-field defaults; new ``HeaderPills`` primitive renders "● UNPROFITABLE · TTM" / "⚠ LIQUIDITY WATCH" / "● BOTTOM DECILE BEAT RATE" / "▲ DEBT RISING · CASH FALLING" pills above the hero (null when healthy). ``HeroCard``'s right-column trio swaps when ``is_unprofitable_ttm``: Forward P/E → P/Sales, ROIC → Cash Runway, FCF margin colored red when negative. Main bundle 97.17 → 97.92 KB gz (+0.75). 605/605 backend + 371/371 frontend tests.
  - **4.5.B done (PR #58 — card adaptations + section reordering + LLM signal feed):** EarningsCard renders a "● BOTTOM DECILE" pill beside its beat-consensus headline when ``beat_rate_below_30pct``. CashAndCapitalCard grows a Cash Runway stat tile (with "raise likely needed" sub-line + red coloring when runway < 6Q) when ``cash_runway_quarters`` is non-null. QualityCard's ROE / ROIC / FCF margin rings flip to red accent when their value is negative. SymbolDetailPage swaps rows 3/4 when distressed (``is_unprofitable_ttm`` OR runway < 6) so Cash + Risk + Macro lift above Valuation + Growth — the survival story comes first. ``_build_user_prompt`` accepts ``layout_signals`` and emits a framing-context block when distressed so Sonnet's narratives adapt their tone (skips the block entirely on healthy reports to save input tokens). Main bundle 97.92 → 98.37 KB gz (+0.45). 607/607 backend + 387/387 frontend tests.
  - **4.5.C done (PR #59 — layout polish from dogfood feedback):** SymbolDetailPage container ``max-w-6xl`` → ``max-w-screen-2xl`` (1152→1536px) so dashboards breathe at 1920px+ resolutions; padding shrinks on smaller breakpoints. Every multi-column row grows ``items-start`` so cards render at natural height with honest gaps below shorter cards (no more stretch-fill blank space under ValuationCard's peer scatter vs PerShareGrowthCard's full multi-line + pills + CAGRs combo). ContextBand moved from between hero and row 2 to below row 4 so numeric content sits up top and narrative context lands at the bottom. RiskDiffCard + MacroPanel return ``null`` instead of placeholder "UNAVAILABLE" cards on reports without 10-K / FRED data; SymbolDetailPage's row 4 auto-collapses to ``data-card-count``-driven 1/2/3 column grid. Main bundle 98.37 → 98.42 KB gz (+0.05). 607/607 backend + 392/392 frontend tests.
  - **4.6.A done (PR #61 — Compare page, visual-only):** new ``/compare?a=X&b=Y`` route, lazy-loaded as a single chunk so the entire compare/ folder ships separately from main. ``ComparePage`` reads ?a + ?b, redirects to landing on missing params, and fires both fetchResearchReport calls in parallel via TanStack ``useQueries``. Renders ``CompareHero`` (two ticker cards + "VS" badge + per-side mini area chart, with HeaderPills appearing only on the distressed column when one side is unprofitable), ``CompareMetricRow`` for Valuation (3 metrics: P/E FWD · P/S · EV/EBITDA, lower=cheaper hint) and Quality (4 metrics: Gross / Op / FCF margin / ROIC, higher=better hint), ``CompareMarginOverlay`` (operating margin 20Q both tickers via MultiLine), ``CompareGrowthOverlay`` (5Y per-share Rev + FCF rebased to 100, multiplier pills below), ``CompareRiskDiff`` (10-K risk diff side-by-side, per-category bars when 4.3.B's Haiku categorizer ran on the side, aggregate fallback otherwise), and ``CompareFooter`` ("What survives the compare" / "What's cut" honest-scope strip). Add-ticker button is rendered disabled (real search modal lands in 4.7); Swap button mutates ``?a`` and ``?b`` in place. New extractor module ``lib/compare-extract.ts`` with 5 helpers + 2 accent-color exports. PEG metric deliberately omitted (not currently a backend claim — revisit in 4.6.B if dogfood asks). Compare narrative ("NVDA's op margin overtook AVGO's in Q2-23") deferred to 4.6.B. Main bundle 98.42 → 98.56 KB gz (+0.14); new ComparePage chunk 6.34 KB gz; SectionChart chunk unchanged at 103.01 KB gz. 607/607 backend + 414/414 frontend tests.
  - **4.7 done (PR #62 — Search + Watchlist + Recent + bundle hygiene):** activates the three sidebar nav buttons that were visually present but disabled since 4.0. New ``lib/watchlist.ts`` + ``lib/recent.ts`` localStorage CRUD helpers (uppercased tickers, dedup, MRU cap at 10 for recent; corrupt-payload survival on both). New ``WatchlistButton`` (★ toggle on /symbol/:ticker, local-mirrored aria-pressed state). New ``SearchModal`` (⌘K-triggered globally + sidebar Search button; filterable input over recent ∪ watchlist ∪ POPULAR_TICKERS deduped; Enter submits uppercased; Esc closes; lazy-loaded). New ``lib/popular-tickers.ts`` with 12 curated names (NVDA / AAPL / MSFT / GOOGL / AMZN / META / TSLA / AVGO / JPM / JNJ / WMT / COST). Backend ticker-search endpoint deferred (4.7.B if dogfood asks). ``SidebarShell`` extends with optional ``onCompareClick`` / ``onWatchlistClick`` / ``onRecentClick`` + ``watchlistCount`` / ``recentCount`` props — each non-Search button's enabled state derives from handler presence (backward-compat with pre-4.6 callers). Count badges render ``data-badge='watchlist-count'`` / ``recent-count`` when count > 0. ``AppShell`` adds the global ⌘K listener + re-reads localStorage counts on every route change so badges stay live. ``SymbolDetailPage`` calls ``pushRecent`` on report-success (typos / 404 tickers stay out of recent) and renders ``WatchlistButton`` next to HeaderPills. ``LandingPage`` grows Recent Tickers + Watchlist sections above the search bar — click-to-navigate ticker-card grids, hidden when empty. **Bundle hygiene companion change:** ``SymbolDetailPage`` now lazy-loaded too (mirroring ComparePage pattern from 4.6.A) — moves all per-card Strata components out of main into a ~14.5 KB on-demand chunk. ``risk-extract.ts`` auto-extracted by Vite into a shared chunk (used by both SymbolDetailPage and ComparePage). Main bundle **98.56 → 83.02 KB gz (−15.54)**; new SymbolDetailPage chunk 14.49 KB gz; new SearchModal chunk 1.31 KB gz; new risk-extract shared chunk 4.27 KB gz; ComparePage + SectionChart unchanged. Headroom under 100 KB budget jumps from 1.44 → **16.98 KB**. 607/607 backend + 447/447 frontend tests.
  - 4.8 = Vercel deploy + dogfood gate. Phase 5/6 deferred to dogfood signal.
- **Phase 5 deferred (narrative layer):** Bulls Say / Bears Say with `claim_refs`, What Changed, `?focus=thesis`. Lands only if Phase 4.8 dogfooding shows the dashboard's inline narratives don't satisfy the bull/bear-case need.
- **Phase 6 deferred (XBRL Tier 2):** segment + geography revenue breakdowns, RPO. Conditional on Phase 4.8 signal.
- **Indefinitely deferred** per ADR 0004: `search_history` (pgvector RAG), `compute_options` (yfinance + IV snapshots), Reddit sentiment, real auth + per-user cost caps.

**Tables today:** `symbols`, `news_items`, `news_symbols`, `candles`, `research_reports`. pgvector + `embeddings` columns are not added until/unless `search_history` un-defers.

**Stubs / 501s:** `/v1/analysis`, `/v1/reports/daily/latest`, `/v1/forecasts/{symbol}` are legacy v1 routes — they'll be removed or redirected to `/v1/research` when convenient. Don't fill them in; prefer adding to `/v1/research`'s shape via the Phase 4 roadmap.

**Phase 4 backend additions (all shipped):**
- ✅ `GET /v1/market/:ticker/prices?range=60D|1Y|5Y` — OHLCV from `candles` (Phase 4.1)
- ✅ News integration via `fetch_news` + Haiku categorizer (Phase 4.4.A)
- ✅ Business descriptions via `fetch_business_info` (Phase 4.4.A)
- ✅ Per-card narrative `Section.card_narrative` (Phase 4.4.B)
- ✅ Adaptive-layout signals `ResearchReport.layout_signals` (Phase 4.5.A)
- ⏸ Logo URL resolution (Clearbit or static map) — deferred; HeroCard renders a letter placeholder, dogfood doesn't surface this as missing yet.

When making changes, prefer extending the existing shape (new claims, new history fields, new sections in the static `SECTION_TO_CLAIM_KEYS` registry) over adding parallel surfaces. The deterministic-everything-except-prose architecture is the discipline — read [`app/services/research_orchestrator.py`](app/services/research_orchestrator.py) before proposing structural changes.
