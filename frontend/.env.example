# Backend base URL. Local dev: http://localhost:8000. Production:
# the Fly.io URL (e.g. https://market-analysis-agent.fly.dev).
# Vite injects VITE_-prefixed vars into the bundle at build time;
# anything else stays server-side (and we have no server side).
VITE_BACKEND_URL=http://localhost:8000
